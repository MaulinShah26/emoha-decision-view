export default function MembershipUnit() {
  return (
    <section className="section border-t border-line">
      <div className="mx-auto max-w-content px-6">
        <p className="eyebrow">03 · The unit is a Membership</p>
        <h2 className="mt-6 display title-underline inline-block">
          [Section title]
        </h2>
        <div className="mt-6 space-y-4 text-md text-ink-muted max-w-[680px]">
          <p>
            [Every membership ties together two people: the Elder (user) and
            the Sponsor (payer, often NRI). Retention depends on both.]
          </p>
        </div>
      </div>
    </section>
  );
}
